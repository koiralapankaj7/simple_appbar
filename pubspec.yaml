name: simpleappbar
description: Simple app bar which is aware of network status.
version: 0.0.1
homepage: https://github.com/koiralapankajgit/simple_appbar.git

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  connectivity: ^0.4.8+6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
